version: v1.0
name: Android
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804
  containers:
    - name: main
      image: 'registry.semaphoreci.com/android:29-node'
blocks:
  - name: Bundle android
    task:
      jobs:
        - name: yarn install and cache
          commands:
            - checkout
            - npm i -g yarn
            - cache restore yarn-$(checksum yarn.lock)
            - yarn install --frozen-lockfile
            - cache store yarn-$(checksum yarn.lock) node_modules
            - cd android && chmod +x gradlew && ./gradlew bundleRelease assembleRelease
            - artifact push job android/app/build/outputs/**
